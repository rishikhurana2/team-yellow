import cv2

class Gui:
	def graphicsUI:
		cv2.namedWindow("Camera Feed", cv2.WINDOW_AUTOSIZE)
		cv2.namedWindow("Threshed Feed", cv2.WINDOW_AUTOSIZE)
		cv2.namedWindow("Contour Image". cv2.WINDOW_AUTOSIZE)
		cv2.namedWindow("HSV Image", cv2.WINDOW_AUTOSIZE)
